spin_version = "1"
authors = ["vojdan"]
description = ""
name = "whatlang"
trigger = { type = "http", base = "/" }
version = "0.1.0"

[[component]]
id = "whatlang"
source = "main.wasm"
[component.trigger]
route = "/..."
[component.build]
command = "cargo build --target wasm32-wasi --release"
